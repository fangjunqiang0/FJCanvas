window.onload = function () {
    var canvas = document.getElementById("canvas");
    var ctx = canvas.getContext("2d");


    // 定时更新画布
    // setInterval(function () {
       
    // }, 20);


    function drawCircle(ctx, x, y, r, bc="#fff") {
        ctx.beginPath();
    }
}